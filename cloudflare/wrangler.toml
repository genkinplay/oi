name = "oi-binance-proxy"
main = "worker.ts"
compatibility_date = "2026-04-01"

# 部署后 worker 默认地址：
#   https://oi-binance-proxy.<your-cf-subdomain>.workers.dev
#
# Secret 设置（敏感，不要写进文件）：
#   npx wrangler secret put PROXY_SECRET
#
# 部署：
#   npx wrangler deploy
